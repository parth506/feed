/**
 * Assets directory.
 *
 * Organize assets as:
 *   assets/
 *     fonts/      — local font files (if not loading from Google Fonts)
 *     icons/      — SVG icon assets
 *     images/     — PNG/JPG/WebP image assets
 *     logo.svg    — primary FeedbackIQ logo
 */

// Re-export asset paths as typed constants for safe usage:
// export const LOGO_URL = new URL("./logo.svg", import.meta.url).href;
